version: '3.4'

services:
  sqlserver:
    image: sqlserver

  login.api:
    image: ${DOCKER_REGISTRY-}loginapi
    build:
      context: .
      dockerfile: Usuario.API/Login.API/Dockerfile

  usuario.get:
    image: ${DOCKER_REGISTRY-}usuarioget
    build:
      context: .
      dockerfile: Usuario.API/Usuario.get/Dockerfile

